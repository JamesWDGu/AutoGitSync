# docker compose 会自动读取同目录下的 .env（可选：也可以直接在 shell 里 export）
#
# 这里只放从 .env 取值的那两个变量；其余开关都在 docker-compose.yml 的 environment 里改。

# 必填：Git 仓库地址
GIT_REPO=https://github.com/your-name/my-configs.git

# 私有仓库需要（公开仓库可留空）
GIT_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
