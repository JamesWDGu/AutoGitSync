# Optional convenience for the repository's Compose example.
# Application configuration is still environment variables only.
GIT_REPO=https://github.com/your-name/my-configs.git

# HTTPS token with repository write permission, including for public repositories.
# Leave empty only when authentication is provided another way or is not needed.
GIT_TOKEN=your-write-token
